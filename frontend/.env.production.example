# FacultyLens frontend — PRODUCTION build variables (used at `npm run build`; only VITE_* values are embedded).
# Never put server-side secrets here: everything in this file ships to the browser.
VITE_API_BASE_URL=https://facultylens.example.edu/api
