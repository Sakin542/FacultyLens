// §14 Analytics — the dashboard scenario already exercises /analytics/overview (hit + forced miss), course analytics
// and the section endpoints; this file exists so the layout from the STEP 43 spec is complete.
export { options, default, handleSummary } from './dashboard.js';
